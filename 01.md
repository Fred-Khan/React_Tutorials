# **React for Beginners: A Step-by-Step Tutorial with TypeScript & Vite (2025)**

**React** is a popular JavaScript library for building fast, interactive user interfaces. In this **beginner-friendly tutorial**, we'll use **Vite** instead of the outdated `create-react-app`. 

In this tutorial, you'll learn how to:
1. set up a React + TypeScript project.
2. Understand the project structure.
3. Write components.
4. Build a simple interactive game.

By the end, you’ll have a **fully working React app**.

---

## **Prerequisites**
- Basic knowledge of HTML, CSS, and JavaScript
- A code editor (eg: **[Visual Studio Code](https://code.visualstudio.com/)**)

---

## **Step 1: Install Node.js and npm**

React relies on **Node.js** and **npm** (Node Package Manager) to manage dependencies and run build tools.

### 1. Download & Install Node.js
Go to: [https://nodejs.org](https://nodejs.org)  
Download the **LTS version** (recommended for most users).

> Install it like any other program on your OS.

### 2. Verify Installation
Open your **terminal** (Command Prompt, PowerShell, or Terminal on macOS/Linux) and run:

```bash
node --version
npm --version
```

**Expected Output:**
```
v20.15.1
10.8.1
```

> If you see version numbers → Success!  
> If not → Reinstall Node.js or restart your terminal.

---

## **Step 2: Create Your First React App with Vite + TypeScript**

We’ll use **`Vite`** — the **next-generation build tool** that’s faster, more modern, and the **recommended way** to start React projects in 2025.

> **Why Vite instead of `create-react-app`?**
> - **Blazing fast dev server** (starts in <1 second)
> - **Native ES modules** (no bundling during development)
> - **Better TypeScript support out of the box**
> - **Smaller bundle size** and **faster production builds**
> - **Actively maintained** — `create-react-app` is **deprecated**

---

### Run This Command:
```bash
npm create vite@latest react-vite-app-01 -- --template react-ts
```

> Outside of this exercise, replace `react-vite-app-01` with your preferred project name (e.g., `guess-number-game`, `todo-app`).

### What Happens?
- `npm create vite@latest`: Runs the official Vite project scaffolder
- `--template react-ts`: Chooses **React with TypeScript**
- It creates a clean, modern project structure

You’ll see prompts:
```
√ Use rolldown-vite (Experimental)? ... No
√ Install with npm and start now? ... No
```
> Press **Enter** to accept defaults.

Then:
```
Scaffolding project in .../react-vite-app-01...
Done. Now run:

  cd react-vite-app-01
  npm install
  npm run dev
```

---

### Navigate & Start the App

```bash
cd react-vite-app-01
npm install
npm run dev
```

> **Why `npm install`?**  
> This downloads all dependencies listed in `package.json` into `node_modules/`. Required **once** after creating the project.

> **Why `npm run dev` instead of `npm start`?**  
> Vite uses `dev` for development server. It’s faster and supports **Hot Module Replacement (HMR)** — changes appear **instantly** without full reloads.

This:
- Starts a **local development server**
- Opens `http://localhost:5173` in your default browser
- Enables **HMR** — edit code and see changes live!

You should now see the **Vite + React welcome page** with two spinning logos and a counter.

---

### Pro Tip: Change Default Terminal in VS Code

If you're using **VS Code**:

1. Press `F1` (or `Cmd + Shift + P` on macOS)
2. Type: `Terminal: Select Default Profile`
3. Choose your preferred shell:
   - **Git Bash** (Windows)
   - **PowerShell**
   - **zsh** (macOS)

Now every new terminal opens in your favorite shell!

---

## **Step 3: Explore the Project Structure**

Open your project in **VS Code**:

```bash
code .
```

Here’s what each folder/file does:

| Folder/File | Purpose |
|------------|---------|
| **`public/`** | Static assets (favicon, images) served as-is |
| `vite.svg` | Vite logo used in the app |
| **`src/`** | Where **all your code lives** |
| `assets/` | Images, icons, fonts |
| `App.tsx` | Main component (your app's root) |
| `main.tsx` | Entry point — renders `<App />` into DOM |
| `App.css` | Styles for the App component |
| **`index.html`** | The single HTML page (in project root!) |
| **`vite.config.ts`** | Vite configuration (plugins, server, etc.) |
| **`package.json`** | Project metadata, dependencies, scripts |
| **`tsconfig.json`** | TypeScript configuration |
| **`tsconfig.node.json`** | TypeScript config for Vite |

> **You’ll spend 90% of your time in `src/`**

---

### Key Files Explained

#### `index.html` (Project Root)
```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Vite + React</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**Line-by-Line Explanation:**
- `<div id="root"></div>`: Where React mounts your app
- `<script type="module" src="/src/main.tsx">`: Loads your React code using ES modules (fast!)

#### `src/main.tsx`
```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

**Line-by-Line Explanation:**
- `import App from './App.tsx'`: Imports your main component
- `ReactDOM.createRoot(...)`: Creates a root to render React into
- `document.getElementById('root')!`: Gets the `<div id="root">` from `index.html`
- `<React.StrictMode>`: Enables extra warnings during development

#### `src/App.tsx` (This is the default code created by Vite Template, which we'll examine first)
```tsx
import { useState } from 'react'
import reactLogo from './assets/react.svg'
import viteLogo from '/vite.svg'
import './App.css'

function App() {
  const [count, setCount] = useState(0)

  return (
    <>
      <div>
        <a href="https://vite.dev" target="_blank">
          <img src={viteLogo} className="logo" alt="Vite logo" />
        </a>
        <a href="https://react.dev" target="_blank">
          <img src={reactLogo} className="logo react" alt="React logo" />
        </a>
      </div>
      <h1>Vite + React</h1>
      <div className="card">
        <button onClick={() => setCount((count) => count + 1)}>
          count is {count}
        </button>
        <p>
          Edit <code>src/App.tsx</code> and save to test HMR
        </p>
      </div>
      <p className="read-the-docs">
        Click on the Vite and React logos to learn more
      </p>
    </>
  )
}

export default App
```

**Line-by-Line Explanation:**
- `import { useState } from 'react'`: Imports the `useState` hook
- `import reactLogo from './assets/react.svg'`: Imports image as a variable
- `import viteLogo from '/vite.svg'`: Imports from `public/` folder
- `const [count, setCount] = useState(0)`: State for counter
- `<>` and `</>`: Fragment shorthand (no extra DOM node)
- `<a href="..." target="_blank">`: Link with image
- `onClick={() => setCount(...)}`: Updates state on click
- `className="..."`: CSS classes (not `class`)

---

## **Step 4: Write Your First React Component**

Let’s simplify `src/App.tsx` to understand the basics.

### Replace `src/App.tsx` with:

```tsx
import React from 'react';
import './App.css';

function App() {
  const name = "React Learner";

  return (
    <div className="App">
      <header className="App-header">
        <h1>Welcome to React + Vite!</h1>
        <p>Hello, {name}!</p>
        <button onClick={() => alert('Button clicked!')}>
          Click Me
        </button>
      </header>
    </div>
  );
}

export default App;
```

### Line-by-Line Explanation:
- `import React from 'react';`: Imports the React library, necessary for defining components and using JSX.
- `import './App.css';`: Imports the CSS file to style the component.
- `function App() {`: Defines the App functional component.
- `const name = "React Learner";`: Declares a constant variable `name` and sets it to a string value. This is plain JavaScript inside the function.
- `return (`: Begins the JSX return statement.
- `<div className="App">`: Creates a container div with class "App" for styling.
- `<header className="App-header">`: Creates a header section with its own class.
- `<h1>Welcome to React!</h1>`: Adds a level-1 heading with static text.
- `<p>Hello, {name}!</p>`: Adds a paragraph. `{name}` embeds the JavaScript variable `name` into the JSX, so it dynamically inserts "React Learner".
- `<button onClick={() => alert('Button clicked!')}>`: Creates a button. `onClick` is an event handler that runs the arrow function `() => alert('Button clicked!')` when clicked, showing a browser alert.
- `Click Me`: The button's text.
- `</button>`: Closes the button tag.
- `</header>`: Closes the header.
- `</div>`: Closes the div.
- `);`: Ends the JSX.
- `}`: Closes the function.
- `export default App;`: Exports the component for use elsewhere.

Save → Watch **HMR** update instantly!

---

## **Step 5: Understanding JSX (JavaScript XML)**

JSX lets you write **HTML inside JavaScript**. It’s not HTML — it’s a syntax sugar!

### Example: JSX vs JavaScript

```tsx
// JSX (what you write)
return <h1>Hello World</h1>;
```

### Line-by-Line Explanation:
- `return <h1>Hello World</h1>;`: This is a simple JSX return statement. It creates an `<h1>` heading element with the text "Hello World". The whole thing gets converted to JavaScript by React's build tools.

```js
// Transpiled to (what browser runs)
return React.createElement('h1', null, 'Hello World');
```

### Line-by-Line Explanation:
- `return React.createElement('h1', null, 'Hello World');`: This is the pure JavaScript equivalent of JSX. `React.createElement` is a function that takes three arguments: the tag name ('h1'), props (null here, meaning no attributes), and children ('Hello World' text). This is what JSX compiles to under the hood.

### Key JSX Rules

| Rule | Example |
|------|--------|
| One root element | `<div>...</div>` or `<></>` |
| Close all tags | `<img />` |
| `className`, not `class` | `className="btn"` |
| Embed JS with `{}` | `{count}` |
| Comments | `{/* Comment */}` |

---

## **Step 6: Styling Your App**

Edit `src/App.css`:

```css
.App {
  text-align: center;
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  padding: 40px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

.App-header {
  max-width: 600px;
  width: 100%;
}

h1 {
  font-size: 2.8rem;
  margin-bottom: 16px;
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
}

p {
  font-size: 1.3rem;
  margin: 12px 0;
  opacity: 0.95;
}

button {
  margin-top: 24px;
  padding: 14px 32px;
  font-size: 1.1rem;
  font-weight: 600;
  background: #ff6b6b;
  color: white;
  border: none;
  border-radius: 12px;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4px 12px rgba(255, 107, 107, 0.3);
  text-transform: uppercase;
  letter-spacing: 1px;
}

button:hover {
  background: #ff5252;
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 8px 20px rgba(255, 82, 82, 0.4);
}

button:active {
  transform: translateY(0) scale(0.98);
}
```

### Line-by-Line CSS Explanation:

`.App {` : **Targets** any element with `className="App"` in your JSX.

`text-align: center;` : **Centers all text** inside the container (like headings, paragraphs).

`min-height: 100vh;` : Makes the container **at least as tall as the screen** (`vh` = viewport height). Your app fills the full screen.

`background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);` : Creates a **smooth diagonal gradient** from blue (`#667eea`) to purple (`#764ba2`). `135deg` sets the angle.

`color: white;` : Sets **all text color to white** for good contrast on the dark gradient.

`padding: 40px;` : Adds **40 pixels of space inside** the container on all sides (top, right, bottom, left).

`display: flex;` : Turns the container into a **flexbox** — the modern way to layout elements.

`flex-direction: column;` : Stacks children **vertically** (top to bottom) instead of horizontally.

`align-items: center;` : **Centers all children horizontally** within the flex container.

`justify-content: center;` : **Centers all children vertically** — perfect for full-screen centered layouts.

`font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;` : Sets a **clean, modern font stack**. If the first font isn't available, it tries the next.

`}` : Closes the `.App` rules.

<br/>

`.App-header {` : Targets the `<header className="App-header">` element.

`max-width: 600px;` : Prevents content from getting **too wide** on large screens.

`width: 100%;` : Allows it to **shrink on small screens** while filling available space.

`}` : Closes the `.App-header` rules.

<br/>

`h1 {` : Targets all `<h1>` headings.

`font-size: 2.8rem;` : Sets a **large, bold heading** (`rem` = root em, scales with base font size).

`margin-bottom: 16px;` : Adds **space below** the heading.

`text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);` : Adds a **subtle shadow** behind the text for depth. `rgba` = red, green, blue, alpha (transparency).

`}` : Closes the `h1` rules.

<br/>

`p {` : Targets all `<p>` paragraphs.

`font-size: 1.3rem;` : Makes text **larger and readable**.

`margin: 12px 0;` : Adds **vertical spacing** between paragraphs.

`opacity: 0.95;` : Makes text **slightly transparent** for a softer look.

`}` : Closes the `p` rules.

<br/>

`button {` : Styles **all buttons** in the app.

`margin-top: 24px;` : Adds **space above** the button.

`padding: 14px 32px;` : Adds **internal space**: 14px top/bottom, 32px left/right.

`font-size: 1.1rem;` : Makes button text **larger**.

`font-weight: 600;` : Makes text **semi-bold**.

`background: #ff6b6b;` : Sets **coral red background**.

`color: white;` : White text for contrast.

`border: none;` : Removes default browser border.

`border-radius: 12px;` : **Rounds the corners** for a modern pill shape.

`cursor: pointer;` : Changes mouse to **hand icon** on hover.

`transition: all 0.3s ease;` : **Smoothly animates** all changes over 0.3 seconds.

`box-shadow: 0 4px 12px rgba(255, 107, 107, 0.3);` : Adds a **soft red shadow** for depth.

`text-transform: uppercase;` : Makes text **ALL CAPS**.

`letter-spacing: 1px;` : Adds **space between letters** for emphasis.

`}` : Closes the `button` rules.

<br/>

`button:hover {` : Styles applied **when mouse hovers over button**.

`background: #ff5252;` : Changes to **darker red**.

`transform: translateY(-3px) scale(1.05);` : **Lifts button up 3px** and **slightly enlarges** it.

`box-shadow: 0 8px 20px rgba(255, 82, 82, 0.4);` : Makes shadow **larger and stronger**.

`}` : Closes the `button:hover` rules.

<br/>

`button:active {` : Styles when button is **clicked/pressed**.

`transform: translateY(0) scale(0.98);` : **Pushes button back down** and **slightly shrinks** it for a "pressed" feel.

`}` : Closes the `button:active` rules.

---

## **Final Project: Build a Number Guessing Game**

Replace `src/App.tsx`:

```tsx
import React, { useState } from 'react';
import './App.css';

function App() {
  const [randomNumber] = useState(Math.floor(Math.random() * 10) + 1);
  const [guess, setGuess] = useState('');
  const [guessCount, setGuessCount] = useState(0);
  const [message, setMessage] = useState('Guess a number between 1 and 10');
  const [gameOver, setGameOver] = useState(false);

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    const userGuess = parseInt(guess, 10);
    if (isNaN(userGuess) || userGuess < 1 || userGuess > 10) {
      setMessage('Please enter a valid number between 1 and 10');
      return;
    }

    setGuessCount(guessCount + 1);

    if (userGuess === randomNumber) {
      setMessage(`Congratulations! You guessed it in ${guessCount + 1} guesses.`);
      setGameOver(true);
    } else if (userGuess > randomNumber) {
      setMessage('Too high! Try a smaller number.');
    } else {
      setMessage('Too low! Try a greater number.');
    }

    setGuess('');
  };

  const restartGame = () => {
    window.location.reload();
  };

  return (
    <div className="App">
      <header className="App-header">
        <h1>Guess The Number</h1>
        <p>{message}</p>
        {!gameOver ? (
          <form onSubmit={handleSubmit}>
            <input
              type="text"
              value={guess}
              onChange={(e) => setGuess(e.target.value)}
              placeholder="Enter 1–10"
              className="guess-input"
            />
            <button type="submit" className="guess-btn">
              Submit Guess
            </button>
          </form>
        ) : (
          <button onClick={restartGame} className="restart-btn">
            Play Again
          </button>
        )}
      </header>
    </div>
  );
}

export default App;
```

### Add to `src/App.css`:
```css
.guess-input {
  padding: 12px;
  font-size: 18px;
  margin: 15px 0;
  border-radius: 8px;
  border: 2px solid #ccc;
  width: 200px;
  text-align: center;
}

.guess-btn, .restart-btn {
  padding: 12px 24px;
  font-size: 16px;
  background: #4caf50;
  color: white;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  margin-top: 10px;
}

.guess-btn:hover, .restart-btn:hover {
  background: #45a049;
}
```

---
### OPTIONAL:
To **center your content horizontally but NOT vertically**, you need to modify the `body` styles in **`index.css`**.

Right now, this line is causing **both horizontal and vertical centering**:

```css
body {
  ...
  display: flex;
  place-items: center; /* This centers both horizontally and vertically */
  ...
}
```

The `place-items: center` is a shorthand for:
- `align-items: center` → **vertical centering**
- `justify-content: center` → **horizontal centering**

---

### Solution: Replace `place-items: center` with `justify-content: center`

#### Modify `index.css` → `body` rule:

```css
body {
  margin: 0;
  display: flex;
  justify-content: center; /* Only horizontal centering */
  min-width: 320px;
  min-height: 100vh;
}
```

> Remove `place-items: center` entirely and use `justify-content: center` instead.

---

### Why this works:

| Property              | Effect |
|-----------------------|--------|
| `display: flex`       | Enables flexbox |
| `justify-content: center` | Centers children **horizontally** |
| `align-items` (omitted) | Defaults to `stretch`, so content starts from the top → **no vertical centering** |
| `min-height: 100vh`   | Ensures full viewport height, so content appears at the top (vertically) |

---

### Optional: Vertically align to top (extra clarity)

If you want to be **explicit** that content should start from the top:

```css
body {
  margin: 0;
  display: flex;
  justify-content: center;
  align-items: flex-start; /* Explicitly top-align */
  min-width: 320px;
  min-height: 100vh;
}
```

But `align-items: flex-start` is optional — omitting `align-items` achieves the same effect (default is `stretch`, but with flex container, children align to top).

---

### Final Recommended `body` in `index.css`:

```css
body {
  margin: 0;
  display: flex;
  justify-content: center;
  min-width: 320px;
  min-height: 100vh;
}
```

---

### Result:
- Your game will be **horizontally centered**
- It will appear **at the top** of the page (not vertically centered)
- No changes needed in `App.tsx` or `App.css`

---
## **Summary Checklist**

- [x] Installed Node.js & npm  
- [x] Created app with `npm create vite@latest --template react-ts`  
- [x] Ran `npm install` and `npm run dev`  
- [x] Explored modern Vite project structure  
- [x] Wrote components with JSX & TypeScript  
- [x] Used `useState` for interactivity  
- [x] Built a number guessing game

---

## **Resources**

- [Vite Docs](https://vitejs.dev)
- [React Docs](https://react.dev)
- [TypeScript](https://www.typescriptlang.org)
- [VS Code](https://code.visualstudio.com)

---

You should now have a **modern, fast React + TypeScript + Vite** setup.

Try creating an app as your own project to familiarise yourself further with coding in React.
